package org.abitware.docfinder.ui;

import javax.swing.JFrame;

public class LogViewer extends JFrame {
    public LogViewer(JFrame parent) {
        // Basic setup for the LogViewer frame
        setTitle("Log Viewer");
        setSize(600, 400);
        setLocationRelativeTo(parent);
    }
}
